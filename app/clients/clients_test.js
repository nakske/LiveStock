'use strict';

describe('myApp.clients module', function() {

  beforeEach(module('myApp.clients'));

  describe('clients controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clientsCtrl = $controller('clientsCtrl');
      expect(clientsCtrl).toBeDefined();
    }));

  });
});
